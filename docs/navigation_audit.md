# Navigation Audit

Summary:

- Ensured `ProjectDetail` is reachable from `CollaborativeTasksScreen` and main stack.
- Added `AIChatScreen` to main stack for assistant access.
- Recommendation: align mobile stack routes with web route names: `Dashboard`, `MyTasks`, `Projects`, `ProjectDetail`, `AI`.
- Next steps: perform UX pass to ensure deep links and initial route behavior mirror web.
